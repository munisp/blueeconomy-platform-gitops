apiVersion: 1

contactPoints:
  - orgId: 1
    name: blueeconomy-operations
    receivers:
      - uid: blueeconomy-webhook
        type: webhook
        settings:
          url: ${GRAFANA_ALERT_WEBHOOK_URL}
          httpMethod: POST
        disableResolveMessage: false
