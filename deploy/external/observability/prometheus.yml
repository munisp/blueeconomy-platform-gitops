global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: blueeconomy-systemd
    static_configs:
      - targets: ['systemd-exporter:9558']
        labels:
          platform: blueeconomy
          environment: external
          component: systemd
    metrics_path: /metrics
