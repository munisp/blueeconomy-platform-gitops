apiVersion: v2
name: credential-verification
description: >-
  Workstream D seafarer CoC credentialing services (credential-api
  issuer/verifier, seafarer-credential-worker Temporal lifecycle worker).
  Fail-closed: renders only with approved environment values, digest-pinned
  images and ExternalSecrets-backed configuration.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"
