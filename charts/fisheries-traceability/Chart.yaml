apiVersion: v2
name: fisheries-traceability
description: >-
  Workstream E fisheries and cold-chain traceability services (trace-api,
  trace-worker, outbox-publisher). Fail-closed: renders only with approved
  environment values, digest-pinned images and ExternalSecrets-backed
  configuration.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"
