apiVersion: v2
name: security-operations
description: >-
  Detection engine and OpenCTI bridge publishing security.alerts.v1.
  Fail-closed: renders only with approved environment values, digest-pinned
  images and ExternalSecrets-backed configuration.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"
