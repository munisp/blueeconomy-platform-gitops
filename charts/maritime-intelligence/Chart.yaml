apiVersion: v2
name: maritime-intelligence
description: >-
  Workstream F maritime-intelligence services in Deep Blue ISR mode
  (api, isr-worker, outbox-publisher with OUTBOX_SOURCE=isr),
  national-security controlled. Fail-closed: renders only with approved
  environment values, digest-pinned images and ExternalSecrets-backed
  configuration.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"
