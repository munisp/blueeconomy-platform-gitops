apiVersion: v2
name: port-interoperability
description: >-
  Workstream A eCallUp 2.0 booking and S1 port-call services (api,
  ussd-gateway, booking-worker, outbox-publisher). Fail-closed: renders only
  with approved environment values, digest-pinned images and
  ExternalSecrets-backed configuration.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"
