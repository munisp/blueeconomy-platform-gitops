apiVersion: v2
name: temporal
description: >-
  Temporal server (frontend/history/matching/worker services via
  temporalio/server) plus a namespace-bootstrap Job registering the cvff,
  ecallup and ferry workflow namespaces. Fail-closed: digest-pinned images
  and ExternalSecrets-delivered persistence DSN are required.
type: application
version: 0.1.0
appVersion: "1.31.2"
kubeVersion: ">=1.28.0-0"
