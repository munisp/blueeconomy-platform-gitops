# Operator template only. Do not source this file directly or commit populated values.
# Merge the required values into the approved external operator environment through its secret manager.

GRAFANA_ADMIN_USER=
GRAFANA_ADMIN_PASSWORD=
GRAFANA_BIND_ADDRESS=127.0.0.1
GRAFANA_PORT=3001
PROMETHEUS_BIND_ADDRESS=127.0.0.1
PROMETHEUS_PORT=9090
