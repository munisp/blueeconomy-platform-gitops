# dev environment values for charts/data-platform (blueeconomy-platform-env).
# This file is the TEMPLATE layout; the real file lives in the separately
# deployed blueeconomy-platform-env repository at
# environments/dev/data-platform.yaml and is layered by the ApplicationSet
# multi-source $values ref. Every REQUIRED_* value must be replaced with an
# approved environment value before Argo CD can sync the Application; the
# charts fail closed on any missing gate (see charts/data-platform/templates/000-validation.yaml).
#
# Fill in the chart's render-gated values here, e.g.:
#   image.repository / image.digest      - approved registry + immutable sha256
#   externalSecrets.storeRef.name        - the landing zone's ClusterSecretStore
#   storage / network / endpoint values  - per the chart's 000-validation.yaml
REQUIRED_ENVIRONMENT_VALUES: ""
