# Blue Economy Platform GitOps

This repository contains non-secret Kubernetes policy and environment declarations for the hybrid Blue Economy Platform. It does not contain a kubeconfig, cloud credentials, partner endpoints, certificates, TLS private keys, runtime secrets or production data.

The current base applies namespace boundaries and policy source. A rendered manifest is not evidence of deployment. A deployment is verified only when an authorised hybrid target, change approval and target-side evidence are recorded.
